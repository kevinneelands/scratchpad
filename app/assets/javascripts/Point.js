var Point = (function () {
    // going against the recommended practice but this
    // format is more self-documenting
    function Point(x, y) {
        this.x = x;
        this.y = y;
    }
    return Point;
}());
