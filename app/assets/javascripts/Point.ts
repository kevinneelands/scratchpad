class Point {
	
	public x: number;
	public y: number;

	// going against the recommended practice but this
	// format is more self-documenting

	constructor( x: number, y: number ) {
	  this.x = x;
	  this.y = y;
	}


}